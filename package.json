{
  "name": "datalad-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/gui/main.js",
  "description": "Foundational adapter layer for DataLad Desktop MVP",
  "scripts": {
    "start": "electron .",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^42.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
